server:
  port: 8082

spring:
  application:
    name: exam-service
  data:
    mongodb:
      uri: mongodb://localhost:27017/exam-core-db

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
